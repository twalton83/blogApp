<h1> Index Page</h1>
<%- include("partials/header") %>

<% blogs.forEach(function(blog){ %>
    <div>
        <h2> <%=blog.title %></h2>
        <img src=<%= blog.image %>>
        <span><%= blog.created%></span>
        <p> <%=blog.body%></p>
<% }); %>

<%- include("partials/footer") %>